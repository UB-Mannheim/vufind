<?php foreach ($this->data as $cont): ?>
    <a class="internal"
        href="<?= $this->recordLink()->getUrl($cont) ?>"><?= $this->string()->cleanEsc($cont->getTitle()) ?>
    </a>
<?php endforeach; ?>